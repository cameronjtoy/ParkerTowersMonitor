// Copy this file to config.js and fill in your project's values
// (Supabase project settings > API). The anon key is meant to be public --
// it's what RLS policies exist to protect against -- but never put the
// service_role key here or anywhere in docs/.
const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
const PRICE_THRESHOLD = 2200; // matches the alert threshold in the monitor
